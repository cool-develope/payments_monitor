{
  "name": "paymentsmonitor",
  "version": "0.0.1",
  "description": "Payments Monitor",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean",
    "migrate": "npm run build && node migrate.js",
    "start": "npm run migrate && node index.js",
    "test": "jest"
  },
  "author": "cool-developer",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.6.0",
    "pg-format": "^1.0.4"
  },
  "devDependencies": {
    "jest": "^27.0.4",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
